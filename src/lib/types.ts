export interface Track {
  id: string;
  name: string;
  category: string;
  versions: {
    short: string;
    long: string;
  };
}

export interface User {
  id: string; // Convex document ID
  name: string;
  email: string;
  role: 'user' | 'admin';
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id: string; // Firebase Auth UID, also used as Firestore doc ID
    name: string;
    email: string;
    role: UserRole;
}

export type UserRole = 'user' | 'admin';

export interface User {
    id:
